import * as React from 'react';
import { hot } from 'react-hot-loader';

export class SettingsPage extends React.Component {
    render() {
        return (
            <div className="settingspage placeholder">
                <h1>Settings</h1>
                <h2>not yet</h2>
            </div>
        );
    }
}

export default hot(module)(SettingsPage);
