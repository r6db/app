import * as React from 'react';
import { hot } from 'react-hot-loader';

export class FavoritesPage extends React.Component {
    render() {
        return (
            <div className="favoritespage placeholder">
                <h1>Favorites</h1>
                <h2>not yet</h2>
            </div>
        );
    }
}

export default hot(module)(FavoritesPage);
