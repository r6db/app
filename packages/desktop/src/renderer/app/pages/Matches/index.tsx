import * as React from 'react';
import { hot } from 'react-hot-loader';

export class MatchesPage extends React.Component {
    render() {
        return (
            <div className="matchespage placeholder">
                <h1>Matches</h1>
                <h2>not yet</h2>
            </div>
        );
    }
}

export default hot(module)(MatchesPage);
