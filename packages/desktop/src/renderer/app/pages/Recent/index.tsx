import * as React from 'react';
import { hot } from 'react-hot-loader';

export class RecentPage extends React.Component {
    render() {
        return (
            <div className="recentspage placeholder">
                <h1>Recent</h1>
                <h2>not yet</h2>
            </div>
        );
    }
}

export default hot(module)(RecentPage);
